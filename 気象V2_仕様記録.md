# HIMORI 気象V2 仕様記録(気象庁問い合わせ前・開発版)

最終更新: 2026-08-28
対象: `js/weather-v2-flag.js` の `WEATHER_V2_ENABLED` を `true` にした状態(開発版)
ステータス: 一般公開前。気象庁への確認前提の暫定仕様を含む(該当箇所は明記)。

このドキュメントは、HIMORI(薪ストーブ管理PWA)の気象機能を「天気アプリ」ではなく
「火のある暮らしの準備と振り返りを助ける情報」として作り直した開発版(気象V2)の
仕様記録であり、気象庁への問い合わせ準備用の技術メモを兼ねる。まだ気象庁への
問い合わせ文そのものは作成していない。

---

## 1. 基本方針

- 気象庁が発表した予報・実測値は、原則としてその値・表現を変更しない。
- HIMORI独自の天気予報・雨雪判定・最低気温予測・標高差補正は行わない。
- どの公式情報を表示するか、その下にどんな生活提案を添えるかだけをHIMORI側の判断とする。
- 一般公開版として確定したものではなく、`WEATHER_V2_ENABLED` を `false` に戻せば
  既存(気象V2導入前)の挙動にそのまま戻せる。

---

## 2. データソースと技術検証結果

### 2.1 未来の予報

| 用途 | 出典 | 備考 |
|---|---|---|
| 地点予報(気温・降水確率・天気文) | 気象庁「地点予報」`https://www.jma.go.jp/bosai/forecast/data/forecast/{officeCode}.json` | 既存`jma.js`の`fetchJmaDaily`を使用。公式値のみ、Open-Meteo等の数値予報モデルは一切使用しない |
| 気温(5kmメッシュ、参考値) | 気象庁「天気分布予報」`https://www.jma.go.jp/bosai/jmatile/data/wdist/{発表時刻}/none/{対象時刻}/surf/{min_temp_point,max_temp_point}/data.geojson` | 実際に取得・検証済み。緯度経度+数値のGeoJSON形式で公式に配信されている |
| 降水量・天気種別(5kmメッシュ) | (検証したが不採用) | PNG画像タイルのみで数値・カテゴリの構造化データが存在しないことを確認。画像の色から値を逆算するのは発表値の解釈・生成になりかねないため不採用 |

### 2.2 過去の実測値(アメダス)

| 用途 | 出典 | 備考 |
|---|---|---|
| 観測点マスタ | `https://www.jma.go.jp/bosai/amedas/const/amedastable.json` | 全1286地点。90日キャッシュ |
| 観測点ごとの実測値 | `https://www.jma.go.jp/bosai/amedas/data/point/{観測所ID}/{yyyymmdd}_{3時間区切りHH}.json` | 10分間隔、1日8ブロック(00,03,...,21時) |
| データ保持期間 | 実測で確認 | **約9日**(9日前=200 OK、10日前=404を実機で確認。推測ではなく実測値) |

### 2.3 位置情報

| 用途 | 出典 |
|---|---|
| 郵便番号→住所候補 | HeartRails GeoAPI `searchByPostal`(`https://geoapi.heartrails.com/`) 。既存機能(都道府県/市区町村選択)でも使用中の同一サービス。**商用利用条件・料金は利用規約に明記が見当たらず、一般公開判断時に別途確認が必要** |

---

## 3. 「火のある場所」と「季節の記録に使う観測点」の分離

- **火のある場所**(`profile.location`): 郵便番号→HeartRails→緯度経度+気象庁発表区分(office/class10/class15/class20)。未来の地点予報の基準地点。現在地(GPS)は使用しない。
- **季節の記録に使う観測点**(`profile.amedasStation`): 火のある場所から距離順に抽出したアメダス候補からユーザーが選択。過去の実測気温の記録に使う。両者はUI・データ構造上完全に別概念として扱う。

### 3.1 アメダス候補抽出・おすすめアルゴリズム

1. 火のある場所から60km圏内の観測点を、`amedastable.json`から距離順に最大8件抽出。
2. 各候補について**実際にpoint APIを叩いてtempフィールドの有無を確認**(elemsビット列の意味は公式ドキュメントで確証を得られなかったため推測しない)。
3. 気温観測が確認できた最大5件を候補として提示。
4. **HIMORIおすすめ = 気温観測が確認できた候補のうち最も近い1件**。標高は各観測点の絶対標高を参考情報として表示するのみで、火のある場所との標高差は計算・スコアリングに使わない(火のある場所自体の標高を信頼できる方法で取得できていないため)。将来、信頼できる標高取得手段が得られた場合に備え、`recommendStation(candidates, { fireSiteElevationM })` という拡張引数だけ用意してある(現状は未使用)。

表示形式:

```
HIMORIおすすめ
世羅
火のある場所から 約7.7km
標高 350m(参考)
[世羅を使う]

ほかの候補
本郷  約14.8km / 標高331m(参考)
...
```

補足文言(常時表示): 「火のある場所からの距離を参考にした候補です。観測点の気温は、火のある場所そのものの気温ではありません。」

### 3.2 大和町での実機検証結果(郵便番号7291404)

| 観測所 | 距離 | 標高 | 気温観測 |
|---|---|---|---|
| **世羅(HIMORIおすすめ)** | 約7.7km | 350m | ○ |
| 本郷 | 約14.8km | 331m | ○ |
| 府中 | 約24.0km | 70m | ○ |
| 竹原 | 約25.8km | 5m | ○ |
| 福山 | 約28.4km | 2m | ○ |
| (参考)安宿 | 約9.9km | 363m | ×(気温観測なし、候補から除外) |

---

## 4. ホームに表示する気象カード

表示条件は以下の優先順位で、常に**1枚だけ**表示する(該当なしなら何も表示しない):

1. **雪**: 地点予報の`weatherText`に「雪」を含む(「雨」も含む場合は「雨または雪」とし、勝手に「雪」に丸めない)
2. **最低気温5℃以下**: 公式最低気温をそのまま表示(「氷点下」「強い冷え込み」等のラベルは付けない)
3. **【暫定・実験的】降水確率70%以上の雨**: `profile.rainCardEnabled`(既定OFF)がONの時のみ判定対象になる。雪・低温の判定とは完全に独立

雪または雨カードに低温(5℃以下)も該当する場合は、同じカードに補助行として同居させ、カードを2枚出さない。

各カードには気象庁の地域別予報ページへのリンク(「気象庁発表 ↗」)を付ける。

### 4.1 雨カードの扱い(2026-08-28修正で暫定化)

- 降水量(mm)の公式値が地点予報・5kmメッシュのどちらにも存在しないことを確認したため、気象庁が10%刻みで発表する**降水確率**を条件として使うことを検討している。
- **この降水確率ベースの判定は気象庁への確認前の実験的機能であり、70%という数字を含め「意味のある雨」「まとまった雨」等のHIMORI正式な気象区分として定義していない。**
- `profile.rainCardEnabled`(既定`false`)で個別にON/OFFできる。設定画面の「雨カード(実験的)」トグルから変更可能。OFFの間は雪・最低気温のカードのみ表示される。
- 気象庁への確認事項: 「公式の降水確率を、情報を表示するかどうかの条件として使用し、その下に薪に関する生活提案を表示すること」の可否。

---

## 5. 予報キャッシュの有効性判定(2026-08-28追加)

「古い天気を新しい天気として見せない」を基本方針とする。

- キャッシュ(`weatherCache`)は `fetchedAt`(HIMORI側の取得日時)と `reportDatetime`(気象庁がこの予報を発表した日時、地点予報JSONの`reportDatetime`フィールドをそのまま保存)を持つ。
- 表示直前に `isWeatherCacheValid(weather)` で有効性を判定する。判定基準:
  1. 取得(`fetchedAt`)から24時間を超えて経過していないか
  2. 「明日」の日付がキャッシュの`daily`配列に実在するか(オフラインが数日続くと、古いキャッシュの日付が全て過去になり、①だけでは拾いきれないため)
- 無効と判定された場合、気象カード・天気ストリップ・「詳しい天気を見る」の折りたたみトグルまで含めて**静かに非表示**にする。エラー表示・不安を煽る文言は一切出さない。
- 天気起因の通知(`maybeNotifyWeather`)も同じ判定を通してから送る(ホームに何も出ていないのに通知だけ来る、という不整合を避けるため)。

---

## 6. アメダス実測値の確定方式(2026-08-28修正)

以前は「当日ここまでの実測値」から最低・最高を都度計算して`weatherHistory`にそのまま記録しており、日中に一度でも記録すると、その後さらに気温が下がっても反映されないまま「最終値」のように扱われる問題があった。

### 6.1 現在の方式

- **当日分**: `fetchAmedasTodaySummary`が「ここまでの実測」から最低/最高を計算し、`confirmed: false` として記録する(まだ一日が終わっていないため未確定)。
- **前日以前**: `fetchAmedasConfirmedDaySummary`がその日の全8ブロック(00〜21時)の実測値から最低/最高を計算し、`confirmed: true` として記録する。
- **シーズン振り返り**(`season-review.js`)は `confirmed !== false` のデータのみを集計対象にする(`confirmed`フィールドが無い旧来のデータは、確定/未確定の概念が無かった頃の記録として確定扱いにする)。

### 6.2 数日開かなかった場合の補完(バックフィル)

- `listUnconfirmedDates(weatherHistory, sinceDateIso, { maxDays })` が、記録が無い日・`confirmed: false`のまま残っている過去日を古い順に列挙する。
- 対象範囲は「観測点を選んだ日」または「保持期間の下限(現在から約9日前)」の遅い方から「昨日」まで(当日は対象外)。
- **1回のアプリ起動につき最大3日分**だけ確定処理を行う(`AMEDAS_BACKFILL_MAX_DAYS_PER_RUN = 3`)。1日の確定には最大8リクエストかかるため、大量リクエストを避けるための上限。残りは次回以降の起動時に続きから処理される。
- 実機テストで、9日以上前の日付(範囲外)は自動的に対象から除外され、範囲内の未確定日が起動のたびに古い順へ確実に進むことを確認済み。

---

## 7. カレンダー・シーズン振り返りへの反映

- カレンダー日別詳細: 「この日の最低気温 -6.4℃(世羅観測所・気象庁 ↗)」という即物表示のみ。「冷え込みの強い一日でした」等のHIMORI独自の意味づけは行わない。
- カレンダー月凡例の「雪・冷え込み」チップ: 「記録された雪の日」または「実測が氷点下(0℃未満)」という客観的基準で判定(以前使っていた独自の-5℃しきい値は廃止)。
- シーズン振り返り(「この冬と、火の記録」): 火を焚いた日数・使った薪の量・最も低かった最低気温・氷点下だった日数・出会った樹種数を、既存データ(`buildLivingWithWoodEvents`等)の単純集計として表示。新しい永続データ構造は作らない。

### 7.1 月次コメントの生成ロジック(2026-08-28修正)

**変更前のロジック**: 季節内で「月平均最低気温が最も低い月」と「焚いた日数が最も多い月」の両方を計算し、両者が一致する月がある場合だけ「◯月は低い気温の日が多く、薪を使うペースも大きかったようです。」という1文で結んでいた。これは「寒かったから薪を多く使った」という因果関係をHIMORI側が示唆しているように読める可能性があった。

**変更後のロジック**: 「焚いた日数の多さ」との一致判定を廃止。季節内で月平均最低気温が最も低い月だけを特定し、その月について次の2つの事実を独立した文として並べる(一致条件なし、常に両方表示):

1. `◯月は低い気温の日が多い月でした。`(その月の平均最低気温が季節内で最も低いという事実)
2. `◯月の薪使用量は◯m³でした。`(その月の焚いた回数 × 1回あたり消費量目安から算出。その月に焚いた記録が無ければこの行は省略)

因果や相関を一切示唆しない、事実の列挙のみの表現に変更した。

---

## 8. 既存データ互換性

新規フィールドはすべてoptional。`migrateProfile`のデフォルト補完パターンに以下を追加:

- `profile.amedasStation`(既定`null`)
- `profile.rainCardEnabled`(既定`false`)

`weatherHistory[]`エントリの新規フィールド: `stationId` / `stationName` / `source` / `confirmed`(いずれもoptional、無い場合は旧来のデータとして安全にフォールバック)。

`weatherCache`の新規フィールド: `reportDatetime`(optional)。

`exportAllData`/`importAllData`による往復テストを実施し、上記すべてのフィールドが完全に一致して復元されることを確認済み。

---

## 9. feature flag

`js/weather-v2-flag.js`の`WEATHER_V2_ENABLED`(現在`true`)を`false`に戻すと、気象V2導入前の挙動(Open-Meteo併用、都道府県/市区町村選択、48時間安全バナー等)にそのまま戻る。雨カードの`rainCardEnabled`は気象V2の中でさらに独立したサブフラグとして機能する。

---

## 10. テスト結果(2026-08-28修正分)

- アメダス確定方式: 当日分`confirmed:false`、過去分`confirmed:true`での記録を実機データで確認。2回連続実行でバックフィルが前回の続きから進むことを確認(3日分ずつ: 08/19-21 → 08/22-24)。
- 保持期間の実測: 9日前=200 OK、10日前=404を確認し、`AMEDAS_RETENTION_DAYS = 9`として反映。
- キャッシュ有効性判定: 正常な取得直後は有効、`fetchedAt`を3日前に書き換えたキャッシュは無効と判定され、気象カード・天気ストリップ・折りたたみトグルがすべて非表示になることを実機で確認。
- 雨カードON/OFF: 既定OFFで降水確率80%のデータでもカードが出ないこと、ONに切り替えると同じデータでカードが出ることを確認。OFFの状態でも雪カードは正常に表示されることを確認(雪・低温の判定と分離されていることの確認)。
- おすすめ観測点の表示: 実機で「HIMORIおすすめ / 世羅 / 火のある場所から 約7.7km / 標高 350m(参考)」の表示形式を確認。
- シーズン振り返り: 未確定(`confirmed:false`)の極端な値(-20℃)を注入したテストで、「いちばん低かった最低気温」の集計から正しく除外されることを確認。月次コメントが新しい2文形式(「1月は低い気温の日が多い月でした。」「1月の薪使用量は0.78m³でした。」)で出力されることを確認。
- バックアップ/復元往復: `confirmed`・`rainCardEnabled`・`reportDatetime`を含む全新規フィールドが完全一致で復元されることを確認。
- 全画面ナビゲーション・console error: HIMORI起因のエラー0件(新規タブでの検証も含む)。

---

## 11. 変更ファイル一覧(2026-08-28修正分)

`js/amedas.js` / `js/app.js` / `js/jma.js` / `js/render/check.js` / `js/render/home.js` / `js/render/season-review.js` / `js/render/settings.js` / `js/render/sheets.js` / `js/store.js` / `js/weather.js` / `service-worker.js`(`himori-v57`)

---

## 12. 気象庁へ確認すべき事項(未整理・問い合わせ文は未作成)

1. 「ユーザー地点を含む約5km格子を機械的に選択し、その格子の気温をそのまま表示する」方法(`temp_point`)が予報業務許可の対象になるか
2. アメダス実測値をHIMORI上で「季節の記録」として個人利用の範囲で表示・保存することの扱い
3. 地点予報の商用アプリ内表示、および**降水確率を情報表示の条件として使用し、その下に生活提案文言を付加すること**の可否
4. HeartRails GeoAPIの商用利用条件(HIMORI側で確認が必要、気象庁への確認事項ではない)
